{
  "framework": "vite",
  "rootDirectory": "frontend",
  "buildCommand": "npm install --no-bin-links && npm run build",
  "outputDirectory": "dist",
  "env": {
    "VITE_API_URL": "https://microbial-ai.onrender.com"
  }
}
